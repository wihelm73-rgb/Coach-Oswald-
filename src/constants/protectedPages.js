// Pages nécessitant un compte connecté. Utilisé par Sidebar.jsx (cadenas + libellé) et
// App.jsx (blocage de l'affichage) — une seule source pour ne pas désynchroniser les deux.
export const PROTECTED_PAGES = ['dashboard', 'programs', 'tests', 'agenda', 'tutos', 'account'];
